package week4.ds;

import java.util.ArrayList;
import java.util.Collection;
import java.util.LinkedList;
import java.util.List;

public class LinkedLists {
  public static void main(String[] args) {

    int a = 1;
    //object reference: Declared by Interface
    List<String> strings = new ArrayList<>();
    List<String> strings1 = new LinkedList<>();
    //strings.so
    

    Collection<String> string2 = new ArrayList<>();
    string2.add("John");
    string2.add("Apple");
    System.out.println(string2);
    LinkedList<String> stringLinkedList = new LinkedList<>();
    stringLinkedList.add("23");
    stringLinkedList.addFirst("John"); // o(1)
    stringLinkedList.addFirst("John"); // o(1)
    stringLinkedList.addLast("John"); // o(1)


    for (String s : strings)
    while(stringLinkedList.iterator().hasNext()){

    }
        




    strings.add("hello");
    strings1.add("hi");

if (a ==2) {
  strings = new LinkedList<>();
}
strings.add("bye");
  }
}
